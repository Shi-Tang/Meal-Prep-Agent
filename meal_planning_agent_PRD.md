# 🍜 Meal Planning Agent — Product Requirements Document

**版本**: v1.0（对应 MVP 实现）
**日期**: 2026-05-31
**状态**: MVP 已实现，持续迭代中

---

## 1. 产品概述 (Product Overview)

### 1.1 产品目标

构建一个 AI Agent，帮助两人家庭实现：

- **每周一次性备餐**（冷冻存放吃一周）
- 小票拍照 + 手动录入双轨并行，维护食材库存
- 基于川渝口味 + 当前库存，智能推荐每周 3 道菜
- 用户选菜偏好持续学习，优化后续推荐
- 生成下周超市采购清单，确认购买后一键更新库存

### 1.2 用户画像

| 属性 | 描述 |
|------|------|
| 家庭规模 | 2 人 |
| 体重 | 50 kg / 75 kg |
| 活动水平 | 轻中度（每周健身约 3 次，其余时间久坐办公） |
| 饮食频次 | 每人每天 2 餐（午 + 晚），共 28 餐次 / 周 |
| 口味偏好 | 中国川渝风味 |
| 忌口 | 羊肉、鱼肉（无其他忌口，海鲜、内脏均可） |
| 厨房习惯 | 每周集中备餐一次，冷冻分装 |

---

## 2. 核心功能模块 (Core Modules)

---

### Module 1：食材数据库管理 (Pantry Manager)

#### 1.1 录入方式

**当前实现（MVP v1.0）**：

| 方式 | 触发场景 | 操作流程 |
|------|---------|---------|
| **A. 小票拍照识别** | 超市购物后 | 上传小票照片 → Claude Vision 批量识别商品名称/数量/单位 → 用户逐项勾选确认 / 调整类别 → 一键导入库存 |
| **B. 手动添加** | 随时补充单项食材或调料 | 直接在表格中填写食材名、数量、单位、类别 → 实时入库 |
| **C. 采购清单确认导入** | 按上周清单购买完毕后 | 在采购清单中勾选已购商品 → 点击"确认已购，更新库存" → 自动追加入库 |

> **设计原则**：最小化手动输入。小票识别为主要录入方式，识别结果默认全选，用户只需微调类别或取消勾选非食品项目。

**小票识别交互细节**：
- 识别结果以列表展示，每项包含：勾选框 / 商品名 / 数量+单位 / 类别下拉
- 提供「全选」快捷按钮
- 非食品商品（纸巾、清洁剂等）由 AI 自动过滤，不纳入识别结果
- 同名食材重复导入时，自动叠加数量（不新建条目）

**待实现（v1.5+）**：
- 冰箱整体拍照批量识别存量
- 调料拍照识别余量（读瓶身总量 × 视觉估算剩余比例）
- 不透明容器 fallback：提示用户选择"几成满"

#### 1.2 数据库结构

```
食材条目：
├── id                 唯一标识
├── name_cn            中文名（如：鸡腿）
├── name_en            英文名（如：Chicken Thighs）—— 小票识别时自动填入
├── category           蔬菜 / 肉类 / 碳水 / 蛋奶 / 调料 / 其他
├── quantity           当前数量（数值）
├── unit               lb / oz / ml / L / cup / g / kg / 个 / 包 / 瓶 / 盒 / 块 等
└── last_updated       最近更新时间

（v1.5+ 扩展字段）
├── weekly_min_qty     一周备餐最低需求量（调料专属，触发低库存预警）
├── freshness_priority 新鲜度优先级：high（叶菜）/ medium（根茎/豆腐）/ low（冷冻/干货）
└── storage_type       冰箱冷藏 / 冷冻 / 常温
```

#### 1.3 库存更新时机

| 触发事件 | 更新行为 |
|---------|---------|
| 小票拍照导入 | 追加 / 叠加数量 |
| 手动添加 | 追加新条目 |
| 采购清单确认 | 追加勾选商品 |
| （v1.5）菜单确认后 | 按食谱用量自动扣减消耗 |
| （v1.5）调料低库存 | 触发 ⚠️ 预警，列入下周采购清单 |

---

### Module 2：菜单规划引擎 (Menu Planner)

#### 2.1 菜单规格

- 每周推荐 **3 道**川渝风味主菜
- 菜肴完全基于**当前库存食材**，不要求额外采购
- 肉类禁用羊肉、鱼肉（其余均可，含猪/牛/鸡/虾/内脏）
- 每餐结构约束：蔬菜 + 肉类 + 碳水（碳水由用户自行搭配，不在菜单中固定）

#### 2.2 生成架构（3 槽位并行）

菜单生成分两阶段，总体感知速度 < 5s：

```
阶段 1（约 1s）：轻量名称请求
  → 一次 API 调用，获取 N 个菜名（max 200 tokens）
  → 立即渲染 3 个骨架卡占位

阶段 2（并行）：3 个槽位独立生成
  → 每道菜独立 API 调用（max 600 tokens）
  → Promise.all 并发，哪道先完成哪张卡先渲染
  → 同步发起 prep_notes 请求（max 300 tokens）
```

**槽位隔离原则**：每个槽位的 dish 对象写入固定的数组下标，互不影响，彻底避免流式合并导致的覆盖问题。

#### 2.3 食谱 Flip Card

每道菜以翻转卡片呈现：

**正面**（始终可见）：
- 菜名（中文大字 + 英文斜体小字）
- 设备标签 / 时间标签 / 份量标签
- 食材清单（名称 + 用量，美制单位）
- 右上角圆形勾选框
- 底部"点击查看做法"提示

**背面**（点击翻转后加载）：
- 懒加载触发：首次翻转时发起 API 请求（max 1200 tokens）
- 5–7 步详细备餐步骤，全部使用美制单位（°F / lb / oz / cup / tbsp / tsp / inch）
- 再次点击标题栏或 × 翻回正面

#### 2.4 备餐前注意

菜单生成后，独立输出简短的备餐前提醒：
- **仅包含**真正需要提前操作的事项：解冻 / 泡血水 / 腌制 / 烤箱预热
- 每条 ≤ 12 字，带 emoji 图标
- 若当周无需提前操作，此模块不显示
- **不包含**烹饪建议、分装提示、复热方法等（这些属于烹饪常识，无需提示）

#### 2.5 烹饪设备约束（硬性）

| 可用设备 | 典型菜式 |
|---------|--------|
| 炒锅 (Wok) | 水煮、干锅、爆炒、回锅肉 |
| 平底锅 (Skillet) | 煎肉饼、煎豆腐 |
| 炖锅 (Dutch Oven) | 红烧、炖汤 |
| 高压锅 (Instant Pot) | 炖肉、豆类、卤制 |
| 微波炉 (Microwave) | 辅助加热、蒸制 |
| 烤箱 (Oven) | 烤鸡腿、烤蔬菜 |

---

### Module 3：菜品偏好学习 (Preference Engine)

#### 3.1 偏好记录机制

用户在菜单页对每道菜进行勾选，确认后系统记录偏好：

| 操作 | 记录结果 |
|------|---------|
| 勾选某道菜 → 确认 | 该菜记入**喜欢列表** |
| 未勾选某道菜 → 确认 | 该菜记入**不喜欢列表** |
| 「全部重换」 | 当前所有菜记入**不喜欢列表** |

#### 3.2 选菜交互流程

```
菜单生成完毕（3张卡全部加载）
    │
    ▼
用户勾选想做的菜（右上角圆形勾）
    │
    ├─ 勾选了 3 道 → 按钮变「🛒 去采购 →」→ 直接进入采购清单
    │
    └─ 勾选了 1-2 道 → 点击「✓ 确认选择」
            │
            ├─ 已勾选：记为喜欢，保留原卡不动
            └─ 未勾选：记为不喜欢，对应槽位重新生成新菜
```

**保留机制**：重新生成时，已选菜在其原始槽位保持不动，只有被淘汰的槽位异步生成新菜，确认栏显示「已选 N/3 道菜」实时更新。

#### 3.3 偏好影响推荐

每次 `generateMenu` 调用时，将偏好历史注入 prompt：

```
用户喜欢的菜（优先推荐）：水煮牛肉、红烧鸡腿、...（最近10条）
用户不喜欢的菜（尽量不推荐）：麻婆豆腐、蒜苔炒肉、...（最近15条）
```

菜单标题栏旁显示最近偏好标签（❤ 水煮牛肉 / ✕ 麻婆豆腐），方便用户感知学习效果。

#### 3.4 数据存储

- **MVP**：session 内存存储（`prefStore` 对象），刷新页面后重置
- **v1.5**：持久化到本地 JSON / 云端数据库，跨周积累

---

### Module 4：食谱详情 (Recipe Detail)

食谱以懒加载方式在 flip card 背面呈现，**全部使用美制单位**：

| 类型 | 使用单位 |
|------|---------|
| 温度 | 华氏度 °F |
| 体积 | cup / tbsp / tsp / fl oz |
| 重量 | oz / lb |
| 长度 | inch |

步骤要求：
- 5–7 步，每步具体可操作
- 包含处理时间、火候、温度
- 最后一步统一为「分装入容器，冷却后冷冻保存」
- 不包含营养信息（已移除）

---

### Module 5：超市采购清单 (Shopping List)

#### 5.1 采购逻辑

采购清单面向**下周备餐**，不是本周差额补货：

```
下周采购建议 = 参考本周菜单消耗 + 当前剩余库存分析 → 推算下周所需补购
```

#### 5.2 超市推荐

从 Trader Joe's / Whole Foods / Target 中推荐覆盖度最高的一家作为主力超市：
- **只显示超市名称**，不展示推荐理由
- 特殊川渝调料（干花椒、干辣椒、醪糟、豆豉等）单独列入中超清单

**三家超市品类基准知识**（通过 Web Search 建立，可动态更新）：

| 品类 | Trader Joe's | Whole Foods | Target |
|------|:-----------:|:-----------:|:------:|
| 基础肉类 | ✅ 实惠 | ✅ 品质高 | ⚠️ 有限 |
| 常规蔬菜 | ✅ | ✅ 品种多 | ⚠️ 有限 |
| 特殊蔬菜（蒜苔等） | ❌ | ⚠️ 偶尔 | ❌ |
| 豆腐 | ✅ | ✅ | ⚠️ |
| 生姜/大蒜/香菜 | ✅ | ✅ | ⚠️ |
| 酱油/香油/米醋 | ✅ | ✅ | ✅ |
| 豆瓣酱 (Doubanjiang) | ❌ | ✅ 已确认 | ❌ |
| 干辣椒 / 花椒 | ❌ | ⚠️ 偶尔 | ❌ |
| 醪糟/豆豉等发酵类 | ❌ | ❌ | ❌ |

#### 5.3 购物清单交互

清单以可交互 checklist 形式呈现，方便用户在超市边走边勾：

- 每项点击切换已购/未购状态（已购项变灰 + 划线）
- 顶部进度条显示「N / 总数 已购」
- 按分区排列：🥩 肉类 / 🥦 蔬菜&豆制品 / 🌾 碳水&干货 / 🧂 调料
- 中超商品单独分区，底部展示
- 调料低库存项标注 ⚠️

#### 5.4 确认采购 → 更新库存

```
用户在超市勾选已购商品
    ↓
点击「✅ 确认已购 N 件，更新食材库存 →」
    ↓
勾选商品自动写入食材库存（同名叠加数量，新品追加条目）
    ↓
自动跳转回「食材库存」页，显示更新结果
```

---

## 3. 完整工作流 (End-to-End Workflow)

### 3.1 每周使用流程

```
【Step 1 — 食材库存】
    │
    ├─ A. 拍照小票 → AI 识别 → 勾选确认 → 导入库存
    └─ B. 手动添加单项食材 / 调料
    │
    ▼
【Step 2 — 菜单食谱】
    │
    ├─ 生成菜单：先取菜名（~1s）→ 3 个槽位并行生成（各自独立）
    │   → 骨架卡逐个替换为真实卡
    │
    ├─ 查看食谱：点击卡片翻转 → 懒加载详细步骤
    │
    ├─ 选菜：右上角勾选 → 确认
    │   ├─ 选满 3 道 → 直接去采购
    │   └─ 选不足 3 道 → 保留已选，替换未选 → 重新生成对应槽位
    │
    ├─ 记录偏好：已选 → 喜欢；未选 → 不喜欢
    │
    ▼
【Step 3 — 采购清单】
    │
    ├─ 生成下周采购建议（基于本周消耗推算）
    ├─ 推荐主力超市（名称）
    ├─ 分区 checklist，用户逐项勾选
    └─ 确认已购 → 自动更新食材库存 → 跳回 Step 1
```

### 3.2 已移除的功能（经迭代删除）

| 功能 | 移除原因 |
|------|---------|
| 7天轮换表 | 用户希望自由搭配，无需系统规划 |
| 营养成分显示 | 信息密度过高，用户不关注 |
| 并行备餐时间线详细流程 | 替换为简短的「备餐前注意」提示 |
| 菜单推荐白米饭 | 碳水由用户自行决定搭配 |
| 超市推荐理由说明 | 简化 UI，只显示超市名称 |
| 图片 AI 生成（菜肴配图） | 加载慢，改为纯文字卡片 |

---

## 4. 需求状态总览

| 模块 | 需求点 | 状态 |
|------|-------|------|
| 食材管理 | 小票拍照识别（Claude Vision） | ✅ 已实现 |
| 食材管理 | 识别结果逐项确认 + 类别调整 | ✅ 已实现 |
| 食材管理 | 手动添加食材 / 调料 | ✅ 已实现 |
| 食材管理 | 采购确认后自动追加库存 | ✅ 已实现 |
| 食材管理 | 同名食材自动叠加数量 | ✅ 已实现 |
| 食材管理 | 冰箱整体拍照识别存量 | 🔜 v1.5 |
| 食材管理 | 调料余量识别（瓶身×视觉估算） | 🔜 v1.5 |
| 食材管理 | 调料低库存预警 | 🔜 v1.5 |
| 菜单规划 | 3 道菜，完全基于当前库存 | ✅ 已实现 |
| 菜单规划 | 3 槽位并行生成，逐卡渲染 | ✅ 已实现 |
| 菜单规划 | 骨架卡占位，流式体验 | ✅ 已实现 |
| 菜单规划 | 川渝口味，忌口过滤 | ✅ 已实现 |
| 菜单规划 | 6 种设备约束 | ✅ 已实现 |
| 菜单规划 | 备餐前注意（仅解冻/腌制/预热） | ✅ 已实现 |
| 偏好学习 | 勾选菜肴记录喜欢/不喜欢 | ✅ 已实现 |
| 偏好学习 | 选不足 3 道时保留已选、替换未选 | ✅ 已实现 |
| 偏好学习 | 偏好历史注入 prompt 影响推荐 | ✅ 已实现 |
| 偏好学习 | 偏好标签显示（UI 感知） | ✅ 已实现 |
| 偏好学习 | 持久化存储（跨周积累） | 🔜 v1.5 |
| 食谱详情 | Flip card 翻转展示 | ✅ 已实现 |
| 食谱详情 | 懒加载步骤（首次翻转触发） | ✅ 已实现 |
| 食谱详情 | 全美制单位 | ✅ 已实现 |
| 采购清单 | 面向下周备餐推算 | ✅ 已实现 |
| 采购清单 | 推荐主力超市（名称） | ✅ 已实现 |
| 采购清单 | 按分区排列的 checklist | ✅ 已实现 |
| 采购清单 | 中超特殊调料独立分区 | ✅ 已实现 |
| 采购清单 | 确认已购 → 自动更新库存 | ✅ 已实现 |
| 采购清单 | Web Search 实时验证超市供货 | 🔜 v1.5 |

---

## 5. 技术实现

| 组件 | 当前方案（MVP） | 计划升级（v1.5+） |
|------|--------------|----------------|
| AI 核心 | Claude Sonnet 4（claude-sonnet-4-20250514） | — |
| 图像识别 | Claude Vision（小票 OCR） | 扩展至冰箱/调料瓶识别 |
| 菜单生成 | 3 次并行 API 调用（各 600 tokens） | 缓存常用菜肴减少请求 |
| 食谱步骤 | 懒加载，1200 tokens/道 | — |
| 食材数据库 | React state（session 内存） | SQLite / Supabase |
| 偏好存储 | session 内存（prefStore 对象） | 持久化 JSON / 云端 |
| 超市品类知识 | 内置静态表 | Web Search 动态更新 |
| 前端框架 | React（Claude Artifact） | React Native 移动端 |

---

## 6. 版本规划 (Roadmap)

| 版本 | 核心功能 | 状态 |
|------|---------|------|
| **v1.0 MVP** | 小票识别 + 手动录入 → 菜单生成（3槽并行）+ 偏好学习 + flip card食谱 + 采购清单checklist + 库存回写 | ✅ 已完成 |
| **v1.5** | 持久化数据库 + 冰箱拍照识别 + 调料余量识别 + 低库存预警 + 偏好跨周积累 | 🔜 规划中 |
| **v2.0** | Web Search 实时超市验证 + 库存消耗自动扣减 + 历史菜单记忆（跨周去重） | 🔜 规划中 |
| **v2.5** | 移动端 App + 营养周报（可选）+ 多人协作 | 🔜 规划中 |

---

*v1.0 — 对应 MVP 实现状态，已移除7天轮换表、营养成分、详细备餐时间线*
