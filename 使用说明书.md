# 🍜 备餐规划师 · 产品使用说明书

> 版本：对应当前线上实现（Vite + React 18 + Vercel Serverless + Google Gemini 2.5 Flash-Lite）
> 适用人群：日常使用者 + 想要自定义（改菜谱 / 改个人信息）的用户

---

## 目录

1. [产品是什么](#1-产品是什么)
2. [日常使用：三步流程](#2-日常使用三步流程)
3. [我要改东西：去哪里改（重点）](#3-我要改东西去哪里改重点)
   - [3.1 界面直接设置（几人份 / 菜系 / 菜品数 / 忌口）](#31-界面直接设置几人份--菜系--菜品数--忌口)
   - [3.2 修改源码里仍写死的设定（厨具 / 备餐频率 / 单位）](#32-修改源码里仍写死的设定厨具--备餐频率--单位)
   - [3.3 修改默认食材库存](#33-修改默认食材库存)
   - [3.4 修改菜谱（做法）与库存匹配规则](#34-修改菜谱做法与库存匹配规则)
4. [本地运行与部署](#4-本地运行与部署)
5. [常见问题 FAQ](#5-常见问题-faq)
6. [文件结构速查](#6-文件结构速查)

---

## 1. 产品是什么

一个面向两人家庭的 **备餐助手**，帮你完成「每周一次性备餐、冷冻吃一周」的全流程：

| 能力 | 说明 |
|------|------|
| ⚙️ 偏好设置 | 界面直接设置「几人份 / 菜系 / 每周菜品数 / 忌口」，无需改代码 |
| 🥕 食材库存 | 拍小票 **AI 识别**录入（英文小票会译为规范中文名）/ 手动添加 / 采购后一键回写 |
| 🍳 菜单食谱 | **本地硬匹配**当前库存与 `recipes.js` 菜谱库，即时出菜单；翻卡看详细做法 |
| ♥ 偏好学习 | 收藏喜欢的菜，后续推荐会更常出现 |
| 🛒 采购清单 | 根据菜单卡片上的 **缺料** 自动汇总，分区勾选，确认后写回库存（不调 AI、不推荐超市） |
| 📅 日历同步 | 把采购清单导出为 `.ics`，一键加进 iPhone 日历（标题「备餐采购 🛒」） |

界面顶部有三个步骤标签：**① 食材库存 → ② 菜单食谱 → ③ 采购清单**，按顺序走即可。

### 与旧版说明的主要区别

| 项目 | 当前实现 |
|------|----------|
| 菜单生成 | **不调用 AI**，按库存与菜谱库规则匹配（`menuInventory.js`） |
| 采购清单 | **不推算「下周」**，只汇总第 ② 步菜单里标注为「仍缺」的食材 |
| 超市推荐 | **已移除**（无 Whole Foods / 中超分区等） |
| 菜谱库规模 | 当前收录 **18 道**（均有 `prep` 调料行）；仅库内菜可匹配出菜单 |
| API 用途 | **仅小票识别**走 Gemini；菜单 / 采购 / 翻卡做法均本地完成 |

---

## 2. 日常使用：三步流程

### 第 1 步 · 食材库存（🥕）

> ⚙️ **页面顶部有「备餐偏好设置」卡片**：在这里直接设「几人份 / 菜系 / 每周菜品数 / 忌口」，改完点「生成本周菜单」即生效。详见 [3.1](#31-界面直接设置几人份--菜系--菜品数--忌口)。

有三种入库方式，可混用：

1. **拍照导入小票**（唯一会调用 AI 的步骤）
   - 点「点击上传超市小票照片」→ 选一张小票照片
   - 浏览器会先把图片压缩（最长边 ≤ 1600px），再交给 **Gemini** 识别
   - 识别结果会 **规范为简体中文食材名**（如 `chicken thigh` → `鸡腿`，`beef brisket` → `牛腩`）；不同部位不会合并
   - 结果默认全选，逐项可：取消勾选 / 改类别
   - 点「✅ 导入 N 件食材到库存」完成；**同名（规范名相同）食材会自动叠加数量**
2. **手动添加**
   - 首次打开时表格为**空态**（无预填食材），点「+ 添加一行」开始录入
   - 填写：食材名 / 数量 / 单位 / 类别；建议用与菜谱匹配一致的规范名（如「鸡腿」「牛腩」「生抽」）
   - 可继续添加多行，右侧「×」删除一行
3. **采购确认回写**（见第 ③ 步）

确认库存无误后，点「🍳 生成本周菜单 →」。

> 📷 小票照片建议用 JPG / PNG。部分 iPhone 的 HEIC 格式可能无法解码，会提示你换格式。
>
> 💡 若只用手动录入、不用小票，可不配置 `GEMINI_API_KEY`，但无法使用拍照导入。

### 第 2 步 · 菜单食谱（🍳）

- 点击生成后，App **在本地根据库存匹配菜谱**（约 1 秒内完成，非 AI 流式生成）
- 每张卡是**翻转卡**：
  - **正面**
    - 菜名、设备 / 时间 / 份量标签
    - 食材清单（来自菜谱库解析）
    - **库存状态**：
      - `✓ 库存可完整制作`：主料 + 调料需求均能在库存中找到对应规范名
      - `⚠ 系统推荐（库存未凑齐）`：主料大致够、仍缺部分调料或配菜，并列出「缺少：xxx、yyy」
    - 缺料行在清单里会标红并带「（缺）」
  - **背面**（点卡片翻转）
    - **用料**：主料一段 + 若干行 `prep`（每行整段显示，如 `鱼香汁：白糖 20g，陈醋 20ml…`；不会拆成单个调料项，也没有「预调汁」分区标题）
    - **做法**：按步骤编号展示（单位多为中式 g / ml，与步骤文案一致）
  - 右上角 **♡ / ♥**：收藏，收藏后以后会更常被推荐
- 顶部可能出现**黄色提示条**（例如：部分菜仍缺料、或库存无法完整制作任何一道时的说明）
- 全部卡片就绪后，点「🛒 去采购 →」进入采购清单（会按当前菜单汇总缺料）
- 底部还有「← 修改食材」「🔄 重新生成菜单」

> 做法从本地菜谱库（`src/recipes.js`，当前 **18 道**）直接读取，**翻卡不消耗 API**。菜单只会从这 18 道菜里选；菜系偏好会过滤可候选菜（当前库内均为川菜系家常菜）。若匹配不到足够数量的菜，会提示补充库存或调低「每周菜品数」。

### 第 3 步 · 采购清单（🛒）

- **来源**：第 ② 步菜单里 `库存未凑齐` 的菜品所标注的缺料（`missingGroups`），在本地聚合，**不调用 AI**
- **不是**「根据消耗推算下周该买什么」，也**不推荐**具体超市
- 清单按分区排列，例如：🥩 肉类 / 🥦 蔬菜 & 豆制品 / 🌾 碳水 & 干货 / 🥚 蛋奶 / 🧂 调料 / 📦 其他
- 每项可显示「用于：某菜、某菜」；部分规范名附带英文对照（如 `牛腩 — Beef Brisket`）
- 若本周菜单全部可完整制作，会提示：**「本周菜单所需食材已在库存中，无需额外采购。」**
- 边逛超市边点勾，顶部进度条显示「N / 总数 已购」
- 买完点「✅ 确认已购 N 件，更新食材库存 →」：勾选项**按规范名写回库存**并跳回第 ① 步
- 还可「📅 加入 iPhone 日历」：选好星期几 + 时间 → 下载 `.ics` → 用「日历」打开，自动建一条 **「备餐采购 🛒」** 日程（提前 1 小时提醒，清单写在备注里）

---

## 3. 我要改东西：去哪里改（重点）

> ✅ **常用偏好**可在界面里直接改（几人份 / 菜系 / 菜品数 / 忌口），见 [3.1](#31-界面直接设置几人份--菜系--菜品数--忌口)。
>
> **菜谱、规范名表、匹配逻辑**在源码中，改后需重新运行 / 部署（见[第 4 节](#4-本地运行与部署)）。
>
> - 界面可调：第 ① 步顶部「⚙️ 备餐偏好设置」
> - 源码：`src/App.jsx`（界面与默认库存）、`src/recipes.js`（菜谱）、`src/ingredientCanon.js`（规范名）、`src/menuInventory.js` / `src/recipeRequirements.js`（匹配）、`src/shoppingList.js`（采购汇总）

### 3.1 界面直接设置（几人份 / 菜系 / 菜品数 / 忌口）

📍 **位置：第 ① 步「🥕 食材库存」页面顶部的「⚙️ 备餐偏好设置」卡片**

| 设置项 | 说明 | 取值 |
|--------|------|------|
| **几人份** | 每道菜卡片上显示的份量文案 | 任意正整数（默认 2） |
| **菜系** | 优先从该菜系标签下的菜谱里选（库内菜较少时可能放宽） | 不限 / 川菜 / 粤菜 / 湘菜 / 鲁菜 / 江浙菜 / 东北菜 / 西北菜 / 家常菜 |
| **每周菜品数** | 每次最多生成几道菜 | 1–9（默认 3） |
| **忌口** | 自由文本；含「禁xxx」的片段会从候选菜谱中排除 | 如「禁羊肉、禁鱼肉」；留空表示无忌口 |

> 改完后点「🍳 生成本周菜单」即按新设置重新匹配；若已在第 ② 步，可点「🔄 重新生成菜单」。
>
> ⚠️ 当前设置为**内存存储**（session），**刷新页面会还原成默认值**。想改默认值见 [3.2](#32-修改源码里仍写死的设定厨具--备餐频率--单位) 的 `DEFAULT_SETTINGS`。

### 3.2 修改源码里仍写死的设定（厨具 / 备餐频率 / 单位）

📍 **位置：`src/App.jsx` 的 `DEFAULT_SETTINGS` 与 `buildSystem` 函数**

`buildSystem` 目前主要用于**小票识别**时的系统角色设定（菜单生成已不再使用）。界面默认值在 `DEFAULT_SETTINGS`：

```javascript
const DEFAULT_SETTINGS = {
  servings: 2,
  cuisine: "川菜",
  dishCount: 3,
  restrictions: "禁羊肉、禁鱼肉",
};
```

| 想改的项 | 改哪里 |
|---------|--------|
| 界面项默认值 | `DEFAULT_SETTINGS` |
| 菜系下拉可选项 | `CUISINES` 常量 |
| 小票识别相关系统提示（若扩展 AI 用途） | `buildSystem` |

### 3.3 修改默认食材库存

📍 **位置：`src/App.jsx` 的 `useState` 初始数组 `ings`**

首次打开默认为空。若希望预填常用食材，可改初始数组，例如：

```javascript
const [ings, setIngs] = useState([
  { id: 1, name: "鸡腿", qty: "2", unit: "lb", cat: "肉类" },
  { id: 2, name: "生抽", qty: "1", unit: "瓶", cat: "调料" },
]);
```

- `cat` 建议用：`肉类 / 蔬菜 / 碳水 / 蛋奶 / 调料 / 其他`
- `name` 会经 `normalizeInventoryName` 规范（见 `ingredientCanon.js`），与菜谱匹配时按**规范中文名**比对

### 3.4 修改菜谱（做法）与库存匹配规则

#### 菜谱库 `src/recipes.js`

📍 翻卡做法与菜单候选均来自 `RECIPES`。当前 **18 道**：

回锅肉、鱼香肉丝、水煮肉片、麻婆豆腐、蒜泥白肉、盐煎肉、蚂蚁上树、农家小炒肉、宫保鸡丁、口水鸡、土豆烧鸡、尖椒炒鸡、泡椒鸡丁、水煮牛肉、孜然牛肉、干煸牛肉丝、牙签牛肉、番茄牛腩。

> 只有 **`prep` 非空** 的菜会留在库中参与匹配；新增菜时请至少写一行调料/料汁列举。

结构示例：

```javascript
"鱼香肉丝": {
  ingredients: "猪里脊 300g，胡萝卜 100g，黑木耳 50g",
  prep: [
    "鱼香汁：白糖 20g，陈醋 20ml，生抽 15ml，老抽 5ml，清水 30ml，玉米淀粉 5g",
    "小料：泡椒碎 30g，蒜末 20g，姜末 10g",
  ],
  steps: [ "滑肉：…", "炒料：…", … ],
},
```

| 字段 | 含义 | 说明 |
|------|------|------|
| `ingredients` | 主料（一行字符串） | 参与库存匹配 |
| `prep` | 调料 / 料汁列举（字符串数组） | **仅列用量**，行首用 `调料：`、`鱼香汁：`、`宫保汁：` 等前缀；不要写腌制、焯水等操作步骤 |
| `steps` | 做法步骤 | 必填；翻卡背面展示 |

**`prep` 在界面上的展示**：数组里**每一行原样显示一行**（见鱼香肉丝两行：`鱼香汁：…` 与 `小料：…`），不会拆成单个调料，也不会单独说明「需预先调匀」——用户需结合行首名称与步骤文案理解。

**新增一道菜**：在 `RECIPES` 中增加键值对，并保证 `prep` 至少有一行合规调料列举；同时在 `ingredientCanon.js` 中补全所用规范名与英文别名（否则库存里写了也对不上）。

**名称匹配（翻卡查做法）**：忽略空格、括号、标点，支持包含匹配（如「家常回锅肉」可命中「回锅肉」）。

#### 规范名与匹配逻辑（进阶）

| 文件 | 作用 |
|------|------|
| `src/ingredientCanon.js` | 规范中文名、英文小票别名；`normalizeInventoryName` 统一库存名 |
| `src/recipeRequirements.js` | 从 `ingredients` + `prep` 解析「需要哪些规范名」；评估是否够做 |
| `src/menuInventory.js` | 按库存、菜系、忌口、收藏生成菜单；支持「主料够但缺调料」的 partial 推荐 |
| `src/shoppingList.js` | 从菜单 `missingGroups` 聚合采购项 |

根目录 `recipes.txt` 为早期文本备份，**以 `src/recipes.js` 为准**。

---

## 4. 本地运行与部署

### 环境要求

- Node.js 18+
- **仅使用小票识别时**：需要 [Google Gemini API Key](https://aistudio.google.com/app/apikey)（免费层可用 `gemini-2.5-flash-lite`）

### 本地运行

```bash
npm install

# 复制 .env.example 为 .env.local，按需填写：
#   GEMINI_API_KEY=你的key          # 小票识别必填；不用小票可留空
#   GEMINI_MODEL=gemini-2.5-flash-lite   # 可选
#   VITE_POSTHOG_KEY=...            # 可选，分析用

vercel dev
# 或仅前端：npm run dev
```

打开终端提示的地址（默认 `http://localhost:3000`）。

> `GEMINI_API_KEY` / `GEMINI_MODEL` 为**服务端**变量，写在 `.env.local`，不会暴露给浏览器。

### 部署到 Vercel

```bash
npm run deploy
```

在 Vercel **Project → Settings → Environment Variables** 添加 `GEMINI_API_KEY`（若需小票功能）及可选的 `GEMINI_MODEL`、`VITE_POSTHOG_*`。

---

## 5. 常见问题 FAQ

| 问题 | 原因 / 解决 |
|------|------------|
| **「没有与当前库存匹配的菜谱」** | 库内仅 18 道菜，且要求库存规范名能覆盖主料+调料。请用小票规范导入或手动填写「鸡腿」「牛腩」「豆瓣酱」等，见 `ingredientCanon.js`。 |
| **出的菜显示「库存未凑齐」** | 主料大致匹配但缺调料/配菜；可到第 ③ 步看汇总缺料，或补库存后重新生成。 |
| **采购清单为空** | 说明当前菜单全部 `库存可完整制作`，无需补货。若认为不对，检查正面是否误标为可制作，或重新生成菜单。 |
| **「请求过于频繁，已触发接口限流」** | 仅发生在**小票识别**；Gemini 免费层有 RPM 限制，等约 30 秒再试。菜单/采购不占用该配额。 |
| **小票传不上 / 识别失败** | 用 JPG/PNG；检查 `GEMINI_API_KEY`；图片需清晰。 |
| **英文小票识别名不对** | 应在识别阶段译为规范中文名；若仍错，导入后手动改库存名，或扩展 `ingredientCanon.js` 别名。 |
| **刷新后食材 / 收藏 / 偏好没了** | 当前为内存存储；默认食材写进 `ings` 初始值，偏好默认值写进 `DEFAULT_SETTINGS`。 |
| **翻卡「暂无做法记录」** | 菜名未命中 `recipes.js` 中任一键（菜单正常不应出现库外菜）。 |
| **改了源码不生效** | 本地重启 `vercel dev`；线上重新 `npm run deploy`。 |

---

## 6. 文件结构速查

| 文件 | 作用 |
|------|------|
| `src/App.jsx` | 主界面、三步流程、小票 AI、菜单/采购触发 |
| `src/recipes.js` | 菜谱做法库（当前 18 道） |
| `src/ingredientCanon.js` | 食材规范名与中英文别名 |
| `src/recipeRequirements.js` | 菜谱用料解析与库存评估 |
| `src/menuInventory.js` | 本地菜单匹配与 partial 推荐 |
| `src/shoppingList.js` | 缺料聚合为采购清单 |
| `src/appleSync.js` | 导出 iPhone 日历 `.ics` |
| `src/analytics.js` | PostHog 埋点（可选） |
| `api/chat.js` | Vercel 代理 → Gemini（小票等） |
| `.env.local` | 本地密钥 |
| `meal_planning_agent_PRD.md` | 产品需求背景（部分描述可能早于当前实现） |

---

### 一句话总结「改东西去哪里」

> **几人份 / 菜系 / 菜品数 / 忌口** → 界面第 ① 步「⚙️ 备餐偏好设置」
>
> **默认值、默认库存** → `src/App.jsx` 的 `DEFAULT_SETTINGS`、`ings`
>
> **菜谱与 prep 调料行** → `src/recipes.js` 的 `RECIPES`
>
> **小票英文名如何映射、库存名如何规范** → `src/ingredientCanon.js`
>
> **匹配松紧、partial 推荐阈值** → `src/menuInventory.js`、`src/recipeRequirements.js`
