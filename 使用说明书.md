# 🍜 备餐规划师 · 产品使用说明书

> 版本：对应当前线上实现（Vite + React 18 + Vercel Serverless + Google Gemini 2.5 Flash-Lite）
> 适用人群：日常使用者 + 想要自定义（改菜谱 / 改个人信息）的用户

---

## 目录

1. [产品是什么](#1-产品是什么)
2. [日常使用：三步流程](#2-日常使用三步流程)
3. [我要改东西：去哪里改（重点）](#3-我要改东西去哪里改重点)
   - [3.1 界面直接设置（几人份 / 菜系 / 菜品数 / 忌口）](#31-界面直接设置几人份--菜系--菜品数--忌口)
   - [3.2 修改源码里仍写死的设定（厨具 / 备餐频率 / 单位）](#32-修改源码里仍写死的设定厨具--备餐频率--单位)
   - [3.3 修改默认食材库存](#33-修改默认食材库存)
   - [3.4 修改菜谱（做法）](#34-修改菜谱做法)
4. [本地运行与部署](#4-本地运行与部署)
5. [常见问题 FAQ](#5-常见问题-faq)
6. [文件结构速查](#6-文件结构速查)

---

## 1. 产品是什么

一个面向两人家庭的 **AI 备餐助手**，帮你完成「每周一次性备餐、冷冻吃一周」的全流程：

| 能力 | 说明 |
|------|------|
| ⚙️ 偏好设置 | 界面直接设置「几人份 / 菜系 / 每周菜品数 / 忌口」，无需改代码 |
| 🥕 食材库存 | 拍小票自动识别录入 / 手动添加 / 采购后一键回写 |
| 🍳 菜单食谱 | 基于库存与偏好，从本地 **110+ 道**已收录菜谱中优先推荐，翻卡看详细做法 |
| ♥ 偏好学习 | 收藏喜欢的菜，后续推荐会更常出现 |
| 🛒 采购清单 | 推算下周采购，分区勾选，确认后自动更新库存 |
| 📅 日历同步 | 把采购清单导出为 `.ics`，一键加进 iPhone 日历 |

界面顶部有三个步骤标签：**① 食材库存 → ② 菜单食谱 → ③ 采购清单**，按顺序走即可。

---

## 2. 日常使用：三步流程

### 第 1 步 · 食材库存（🥕）

> ⚙️ **页面顶部有「备餐偏好设置」卡片**：在这里直接设「几人份 / 菜系 / 每周菜品数 / 忌口」，改完点「生成本周菜单」即生效，无需改代码。详见 [3.1](#31-界面直接设置几人份--菜系--菜品数--忌口)。

有三种入库方式，可混用：

1. **拍照导入小票**（推荐）
   - 点「点击上传超市小票照片」→ 选一张小票照片
   - 浏览器会先把图片压缩（最长边 ≤ 1600px），再交给 AI 识别
   - 识别结果默认全选，逐项可：取消勾选 / 改类别
   - 点「✅ 导入 N 件食材到库存」完成；**同名食材会自动叠加数量**
2. **手动添加**
   - 首次打开时表格为**空态**（无预填食材），点「+ 添加一行」开始录入
   - 填写：食材名 / 数量 / 单位 / 类别；可继续添加多行，右侧「×」删除一行
3. **采购确认回写**（见第 3 步）

确认库存无误后，点「🍳 生成本周菜单 →」。

> 📷 小票照片建议用 JPG / PNG。部分 iPhone 的 HEIC 格式可能无法解码，会提示你换格式。

### 第 2 步 · 菜单食谱（🍳）

- 进入后会按你设置的菜品数出现对应数量的**骨架卡**占位，菜品边生成边逐张填充
- 每张卡是**翻转卡**：
  - 正面：菜名、设备/时间/份量标签、食材清单
  - 点卡片任意处 → 翻到背面看 **5–7 步详细做法**（美制单位 °F / lb / oz / cup）
  - 右上角 **♡ / ♥**：收藏这道菜，收藏后以后会更常被推荐
- 菜品全部就绪后，点「🛒 去采购 →」进入采购清单
- 底部还有「← 修改食材」「🔄 重新生成菜单」

> 做法从本地菜谱库（`src/recipes.js`，目前 **110 道**）直接读取，**翻卡不消耗 API**、不会触发限流。生成菜单时 AI 会**优先从已收录菜名里选**；你在「备餐偏好」里选的菜系（如粤菜、湘菜）会进一步引导推荐方向。若某道菜未收录，翻卡会显示「📖 暂无做法记录」。

### 第 3 步 · 采购清单（🛒）

- AI 基于本周菜单消耗 + 当前剩余库存，推算**下周**采购建议
- 顶部显示推荐的主力超市（如 Whole Foods）
- 清单按分区排列：🥩 肉类 / 🥦 蔬菜&豆制品 / 🌾 碳水&干货 / 🧂 调料补充
- 特殊中式调料单独列在 **🏮 中超**分区（H Mart / 99 Ranch）
- 边逛超市边点勾，顶部进度条显示「N / 总数 已购」
- 买完点「✅ 确认已购 N 件，更新食材库存 →」：勾选项**自动写回库存**并跳回第 1 步
- 还可「📅 加入 iPhone 日历」：选好星期几 + 时间 → 下载 `.ics` → 用「日历」打开，自动建一条「去 xx 超市购物」日程（提前 1 小时提醒，清单写在备注里）

---

## 3. 我要改东西：去哪里改（重点）

> ✅ **常用偏好现在可以在界面里直接改**（几人份 / 菜系 / 每周菜品数 / 忌口），见 [3.1](#31-界面直接设置几人份--菜系--菜品数--忌口)，无需碰代码。
> 其余更底层的设定（厨具 / 备餐频率 / 单位、以及界面项的默认值）仍在源码里，见 [3.2](#32-修改源码里仍写死的设定厨具--备餐频率--单位)。改源码后需重新运行 / 部署才生效（见[第 4 节](#4-本地运行与部署)）。
> - 界面可调：第 1 步顶部「⚙️ 备餐偏好设置」卡
> - 源码：`src/App.jsx`（系统提示、默认库存）、`src/recipes.js`（菜谱做法）

### 3.1 界面直接设置（几人份 / 菜系 / 菜品数 / 忌口）

📍 **位置：第 1 步「🥕 食材库存」页面顶部的「⚙️ 备餐偏好设置」卡片**

无需改代码，直接在界面里设置即可：

| 设置项 | 说明 | 取值 |
|--------|------|------|
| **几人份** | 每道菜按几人份估算食材用量 | 任意正整数（默认 2） |
| **菜系** | AI 尽量贴近该菜系的口味与做法；与菜谱库中对应分区一致 | 不限 / 川菜 / 粤菜 / 湘菜 / 鲁菜 / 江浙菜 / 东北菜 / 西北菜 / 家常菜 |
| **每周菜品数** | 每次生成几道菜（骨架卡数量随之变化） | 1–9（默认 3，建议 3–5） |
| **忌口** | 自由文本，作为硬约束写进提示 | 如「禁羊肉、禁鱼肉」「禁牛肉、禁香菜」；留空表示无忌口 |

> 改完后点「🍳 生成本周菜单」即按新设置生成；若已生成过菜单，到第 2 步点「🔄 重新生成菜单」即可用新参数重出。设置也会影响采购清单的推算。
>
> 💡 菜品数越多，一次请求生成内容越多，免费层更易触发限流，建议 3–5 道。
>
> ⚠️ 当前设置为内存存储（session），**刷新页面会还原成默认值**。想改默认值见 [3.2](#32-修改源码里仍写死的设定厨具--备餐频率--单位) 的 `DEFAULT_SETTINGS`。

### 3.2 修改源码里仍写死的设定（厨具 / 备餐频率 / 单位）

📍 **位置：`src/App.jsx` 的 `DEFAULT_SETTINGS` 与 `buildSystem` 函数**（约第 223–235 行）

界面设置的**默认值**在 `DEFAULT_SETTINGS`；而厨具 / 备餐频率 / 单位等没放进界面的设定，写死在 `buildSystem` 生成的系统提示里：

```223:235:src/App.jsx
const DEFAULT_SETTINGS = {
  servings: 2,                 // 几人份
  cuisine: "川菜",             // 偏好菜系（"不限" 则不限制）
  dishCount: 3,                // 每周生成几道菜
  restrictions: "禁羊肉、禁鱼肉", // 忌口（自由文本）
};

// 根据用户偏好动态生成系统提示。
function buildSystem(s = DEFAULT_SETTINGS) {
  return `你是专业的备餐规划师。${s.servings}人份家庭，每周备餐一次冷冻存放。`
    + (s.cuisine && s.cuisine !== "不限" ? `偏好菜系：${s.cuisine}，尽量贴近该菜系的口味与做法。` : "")
    + `约束：每餐含蔬菜+肉类（${s.restrictions?.trim() || "无特殊忌口"}）+碳水；设备：炒锅/平底锅/炖锅/高压锅/微波炉/烤箱；单位用美制（°F/lb/oz/cup/tbsp/tsp/inch）；只用当前库存食材。回复中文。`;
}
```

| 想改的项 | 改哪里 | 示例 |
|---------|--------|------|
| **界面项的默认值**（份数/菜系/菜品数/忌口） | `DEFAULT_SETTINGS` | 把 `servings: 2` 改成 `servings: 4` |
| **菜系下拉可选项** | `CUISINES` 常量（约第 220 行） | 增删数组里的菜系名 |
| **可用厨具** | `buildSystem` 里的 `设备：炒锅/…/烤箱` | 加减设备名称 |
| **备餐频率** | `buildSystem` 里的 `每周备餐一次冷冻存放` | 改成 `每周备餐两次` 等 |
| **计量单位** | `buildSystem` 里的 `单位用美制（…）` | 改成公制等 |

> 改完这些源码后，需重新运行 / 部署（见第 4 节）才会生效。

### 3.3 修改默认食材库存

📍 **位置：`src/App.jsx` 的 `useState` 初始数组 `ings`**（约第 501 行）

首次打开 App 时**默认为空**，用户通过小票识别或手动添加录入。若希望每次打开都预填一批常用食材，可改这个初始数组，例如：

```501:502:src/App.jsx
  const [ings, setIngs] = useState([]);
  const [nid, setNid] = useState(1);
```

预填示例（按需增删）：

```javascript
  const [ings, setIngs] = useState([
    { id:1, name:"鸡腿（带骨）", qty:"3", unit:"lb", cat:"肉类" },
    { id:2, name:"西兰花",       qty:"2", unit:"个", cat:"蔬菜" },
  ]);
```

规则：
- 每行一个食材，`id` 必须唯一（顺延即可）
- `cat` 只能用：`肉类 / 蔬菜 / 碳水 / 蛋奶 / 调料 / 其他`
- `unit` 建议用下拉里已有的单位（见 `UNITS` 常量，约第 218 行）：`lb / oz / 个 / 包 / 瓶 / ml / L / cup / g / kg / 片 / 根 / 头 / 束 / 盒 / 块`

> 注意：这只是**默认值**。运行时在界面里手动加的食材，刷新页面后会丢失（当前为内存存储）。如果想让某些食材每次都在，就写进这个默认数组。

### 3.4 修改菜谱（做法）

📍 **位置：`src/recipes.js` 中的 `RECIPES` 对象**

这是做法库，翻卡时直接读取（不调 API）。当前共 **110 道**，大致分布如下（文件内用 `// ── 菜系 ──` 注释分区，便于查找）：

| 分区 | 约道数 | 举例 |
|------|--------|------|
| 川菜（文件开头） | 20 | 回锅肉、水煮牛肉、宫保鸡丁 |
| 粤菜 | 15 | 蜜汁叉烧、豉汁蒸排骨、啫啫滑鸡煲 |
| 湘菜 | 15 | 辣椒炒肉、毛氏红烧肉、湘味小炒黄牛肉 |
| 江浙 | 15 | 东坡肉、狮子头、糖醋里脊 |
| 东北 | 15 | 锅包肉、小鸡炖蘑菇、酱骨架 |
| 西北 | 15 | 大盘鸡、手抓牛肉条、辣爆牛肉 |
| 鲁菜 | 15 | 焦溜肉段、木须肉、把子肉 |

> 偏好选 **「不限」** 或 **「家常菜」** 时，会在上述全库范围内综合推荐，不单独对应某一段注释块。

结构如下：

```13:23:src/recipes.js
export const RECIPES = {
  "回锅肉": {
    ingredients: "五花肉 400g，青椒或大葱 150g，姜 10g，蒜 15g",
    prep: ["调料：郫县豆瓣酱 30g，甜面酱 15g，生抽 10ml，料酒 15ml，白糖 5g，食用油 20ml"],
    steps: [
      "煮肉：五花肉冷水下锅，加姜片和料酒，大火烧开转中火煮 20 分钟，至筷子能扎透。捞出自然冷却 15 分钟，切成 2mm 厚的肉片。",
      "煸炒：热锅下 20ml 油，中火下肉片煸炒 3-4 分钟，至肉片微微卷曲出油。",
      "调味：下豆瓣酱和甜面酱，中火炒 1 分钟出红油。",
      "出锅：下青椒/大葱，加白糖和生抽，大火翻炒 2 分钟至青椒断生即可出锅。",
    ],
  },
```

每道菜的字段：

| 字段 | 含义 | 是否必填 |
|------|------|---------|
| `ingredients` | 主料（一行字符串） | 可选 |
| `prep` | 腌料 / 料汁 / 调料等准备（字符串数组，每条一行） | 可选 |
| `steps` | 做法步骤（字符串数组，按顺序） | **必填** |

**新增一道菜**：在 `RECIPES` 里对应菜系注释块下加一个键值对（或新建注释块），例如：

```javascript
  "干锅花菜": {
    ingredients: "花菜 400g，五花肉 100g，蒜苗 50g",
    prep: ["调料：郫县豆瓣酱 20g，生抽 10ml，干辣椒 5g"],
    steps: [
      "花菜掰小朵焯水 1 分钟捞出沥干。",
      "热锅下五花肉煸出油，下豆瓣酱、干辣椒炒香。",
      "下花菜和蒜苗大火翻炒 3 分钟，加生抽调味出锅。",
    ],
  },
```

**修改 / 删除**：直接改对应键的内容，或删掉整个键值对。

**名称匹配机制（重要）**：
- 查找时会**忽略空格、括号、标点**并做模糊包含匹配。
- 例如 AI 生成「家常回锅肉」也能命中库里的「回锅肉」。
- 菜单生成时会把 `RECIPES` 里全部菜名（`RECIPE_NAMES`）喂给 AI，**优先从已收录的菜里挑**；配合界面「菜系」偏好，更容易出该分区内的菜。
- 你往 `recipes.js` 里加的菜会更容易被推荐；删掉的菜不再优先，翻卡也可能显示「暂无做法记录」。
- 根目录 `recipes.txt` 为早期文本备份，**以 `src/recipes.js` 为准**。

---

## 4. 本地运行与部署

### 环境要求
- Node.js 18+
- 一个 Google Gemini API Key（[Google AI Studio](https://aistudio.google.com/app/apikey) 免费申请）

### 本地运行

```bash
# 1. 安装依赖
npm install

# 2. 配置密钥：复制 .env.example 为 .env.local，填入你的 key
#    GEMINI_API_KEY=你的key
#    （可选）GEMINI_MODEL=gemini-2.5-flash-lite
#    （可选）VITE_POSTHOG_KEY=...   不填则关闭分析

# 3. 启动（前端 + serverless 函数一起跑）
vercel dev
#    或仅前端调试：npm run dev
```

打开浏览器访问终端提示的地址（默认 `http://localhost:3000`）。

> 后端密钥变量（`GEMINI_API_KEY` / `GEMINI_MODEL`）是**服务端**变量，写在 `.env.local`，不会暴露给浏览器。`VITE_` 开头的变量才会进前端。

### 部署到 Vercel

```bash
npm run deploy      # 等价于 vercel --prod
```

并在 Vercel 控制台 **Project → Settings → Environment Variables** 添加：
- `GEMINI_API_KEY`（必填）
- `GEMINI_MODEL`（可选，默认 `gemini-2.5-flash-lite`）
- `VITE_POSTHOG_KEY` / `VITE_POSTHOG_HOST`（可选，分析用）

> 改了任何源码（菜谱 / 个人信息等）后，都要重新 `npm run deploy` 才会更新线上版本。

---

## 5. 常见问题 FAQ

| 问题 | 原因 / 解决 |
|------|------------|
| **「请求过于频繁，已触发接口限流」** | Gemini 免费层有每分钟请求上限。等约 30 秒再试。后端已自带退避重试。 |
| **小票传不上 / 识别失败** | 优先用 JPG / PNG。部分 HEIC 无法解码会提示换格式。图片要清晰、字迹可辨。 |
| **刷新页面后手动加的食材 / 收藏 / 偏好设置没了** | 当前是内存存储（session），刷新即重置。想长期保留默认食材请写进 [3.3](#33-修改默认食材库存) 的默认数组；想改偏好默认值见 [3.2](#32-修改源码里仍写死的设定厨具--备餐频率--单位)。持久化存储在 v1.5 规划中。 |
| **某道菜翻卡显示「暂无做法记录」** | 该菜未收录进 `recipes.js`（库内共 110 道，AI 偶尔仍会推荐库外菜）。按 [3.4](#34-修改菜谱做法) 补录即可。 |
| **选了某菜系但出的菜不太对味** | 确认偏好里菜系与库存食材匹配；可点「🔄 重新生成菜单」多试几次，或往对应菜系分区加更多菜谱。 |
| **改了源码不生效** | 本地需重启 `vercel dev`；线上需重新 `npm run deploy`。 |
| **服务暂时不可用（API 5xx）** | 多为上游 Gemini 抖动或密钥未配置。检查 `GEMINI_API_KEY` 是否正确设置。 |

---

## 6. 文件结构速查

| 文件 | 作用 | 你可能要改的地方 |
|------|------|-----------------|
| `src/App.jsx` | 主界面 + 全部交互逻辑 | 界面「⚙️ 备餐偏好设置」卡（份数/菜系/菜品数/忌口）；源码里 **`DEFAULT_SETTINGS`/`buildSystem`**（默认值与厨具/频率/单位）、**`ings`**（默认库存）、`CUISINES`/`UNITS`/`CATS`（选项） |
| `src/recipes.js` | 本地菜谱做法库（约 110 道，分菜系注释块） | **`RECIPES`**（增删改菜谱） |
| `recipes.txt` | 菜谱文本备份（参考用） | 一般无需改，改 `src/recipes.js` 即可 |
| `src/appleSync.js` | 采购清单导出 iPhone 日历（.ics） | 一般无需改 |
| `src/analytics.js` | PostHog 埋点（可选） | 一般无需改 |
| `api/chat.js` | Vercel serverless 代理（转发到 Gemini） | 换模型默认值 `MODEL` |
| `.env.local` | 本地密钥 / 配置 | `GEMINI_API_KEY` 等 |
| `meal_planning_agent_PRD.md` | 产品需求文档（设计背景） | 参考用 |

---

### 一句话总结「改东西去哪里」

> **改几人份 / 菜系 / 每周菜品数 / 忌口** → 界面第 1 步顶部「⚙️ 备餐偏好设置」卡，无需改代码
> **改上述项的默认值，或改厨具 / 备餐频率 / 单位** → `src/App.jsx` 的 `DEFAULT_SETTINGS` 与 `buildSystem`
> **改默认食材库存** → `src/App.jsx` 的 `ings` 初始数组
> **改菜谱做法** → `src/recipes.js` 的 `RECIPES` 对象
